import React from 'react';

function Work() {
  return <div className="py-32 text-white text-center">Work page</div>;
}

// Optimized for performance by adding React.memo
export default React.memo(Work);
  